const FALLBACK={updated:null,score:null,decision:"Aún no hay suficientes datos normalizados para recomendar cambios de carga.",metrics:{weight:{v:"89,48",u:"kg",label:"Peso",note:"02/09"},fat:{v:"16,7",u:"%",label:"Grasa",note:"BIA · puntual"},muscle:{v:"71,14",u:"kg",label:"Músculo",note:"BIA · puntual"},rhr:{v:"70",u:"lpm",label:"FCR",note:"19/09 · puntual"},hrv:{v:"—",u:"ms",label:"VFC",note:"normalización pendiente"},sleep:{v:"—",u:"h",label:"Sueño",note:"deduplicación pendiente"},steps:{v:"—",u:"",label:"Pasos",note:"deduplicación pendiente"},photos:{v:"30/08",u:"",label:"Body Check",note:"última sesión"}},sources:[["Withings","Sin medición nueva","warn"],["FC / FCR","Disponible","ok"],["VFC","Disponible · normalizar","warn"],["Sueño","Duplicados detectados","warn"],["Pasos","Duplicados detectados","warn"],["Body Check","Sin sesión nueva","warn"]],series:{weight:[90.30,89.48],rhr:[73,75,74,72,69,69,67,66,67,72,72,74,71,68,70,77,81,80,72,69,66,67,70],hrv:[],sleep:[],steps:[]}};
let data=FALLBACK;const $=s=>document.querySelector(s);
function render(){const m=$("#metrics");m.innerHTML=Object.values(data.metrics).map(x=>`<div class="metric ad-card"><b>${x.v}<small>${x.u}</small></b><span>${x.label}</span><small>${x.note||""}</small></div>`).join("");$("#score").textContent=data.score??"—";$("#scoreLabel").textContent=data.score==null?"Esperando datos normalizados":data.score+" / 100";$("#scoreFill").style.width=(data.score??0)+"%";$("#decision").textContent=data.decision;$("#sources").innerHTML=data.sources.map(x=>`<div class="source"><b>${x[0]}</b><span class="${x[2]}">${x[1]}</span></div>`).join("");draw()}
function draw(){const key=$("#chartMetric").value,a=data.series[key]||[],el=$("#chart");if(!a.length){el.innerHTML='<div class="ad-muted">Sin serie fiable todavía.</div>';return}const lo=Math.min(...a),hi=Math.max(...a),span=hi-lo||1;el.innerHTML=a.map((v,i)=>`<i class="bar" title="${v}" style="height:${25+75*(v-lo)/span}%"></i>`).join("")}
async function load(){try{const r=await fetch("./data/latest.json?"+Date.now(),{cache:"no-store"});if(r.ok)data=await r.json()}catch{}render()}$("#chartMetric").onchange=draw;$("#refresh").onclick=load;load();
# Entrenamiento 2.0 · data

`latest.json` será la salida canónica del normalizador de datos.

Principio: raw (Drive/Health Connect/Withings) → deduplicación/normalización → latest.json → UI.

La interfaz no suma exports brutos ni interpreta valores inconsistentes. Hasta disponer de una serie normalizada, muestra el estado como pendiente.